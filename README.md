# web-fundamentals
HTML and CSS coursework: CSS-only country flags, pagelayouts, CV pages and course assignments.

- `flags/` — CSS-only country flags
- `pages/` — page layout exercises (webpages, a chessboard, a personal site)
- `cv/` — CV/resume page variants
- `assignments/` — course assignments 01–07, plus `all/` linking to each
- `archive/` — earlier portfolio site iterations
- `assets/` — shared icons/images used across the pages above
